<% if logged_in? %>
  <nav class="bg-light border-bottom mb-4">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center py-3">
        <div class="d-flex gap-3">
          <%= link_to "Dashboard", admin_root_path, class: "text-decoration-none #{'fw-semibold' if current_page?(admin_root_path)}" %>
          <%= link_to "Articles", admin_articles_path, class: "text-decoration-none #{'fw-semibold' if current_page?(admin_articles_path)}" %>
          <%= link_to "New Article", new_admin_article_path, class: "text-decoration-none #{'fw-semibold' if current_page?(new_admin_article_path)}" %>
        </div>
        <div class="d-flex gap-3 align-items-center">
          <%= link_to "View Site", root_path, class: "text-decoration-none" %>
          <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-sm btn-outline-secondary" %>
        </div>
      </div>
    </div>
  </nav>
<% end %>
